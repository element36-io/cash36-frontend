import React from 'react';
import PropTypes from 'prop-types';

const Step1Tier1Form = () => {
  return <div>Step 1 Tier 1 Form</div>;
};

Step1Tier1Form.propTypes = {
  changeSteps: PropTypes.func.isRequired
};

export default Step1Tier1Form;
